---
title: langChain基础使用
date: 2026-04-09 09:47:05
permalink: /pages/c30386/
categories:
  - LangChain&LangGraph
  - 代码
tags:
  - 
---
## 初始化模型

### 写法

#### V0.x

```python
model = ChatOpenAI(model="gpt-4")
```

#### V1.0

```python
model = init_chat_model("gpt-4o", model_provider="openai")
model = init_chat_model("qwen-max", model_provider="tongyi")
```

### 代码

#### 写死

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="qwen-plus",
    # 硬编码写死
    api_key="你自己的api-key",  # 平台提供的 API-Key
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

response = llm.invoke("你是谁")

print(response)#元数据,里面包含内容content、包含total_tokens
print(response.content)
```

#### 配置环境变量

```bash
QWEN_API_KEY="你的APIkey"
```

```python
from langchain_openai import ChatOpenAI
import os
from dotenv import load_dotenv


# 加载.env文件中的环境变量（指定编码，避免中文乱码）
load_dotenv(encoding='utf-8')

llm = ChatOpenAI(
    model="deepseek-v3.2",
    # 配置进环境变量
    api_key=os.getenv("QWEN_API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

# # 2.提供问题，并调用llm
user_input = input("请输入你所提问的问题：")
response = llm.invoke(user_input)

print(response)#元数据
print()
print(response.content)
```

#### 1.0版本

1. 还有属性：
   1. temperature，温度,**一般模型是0~1，部分模型支持到2**
   2. max_token，最大token数
   3. timeout，请求超时时间
   4. stop，模型在生成时，遇到这些停止词，将立刻停止，用于控制输出的边界
   5. max_retries：最大请求重试次数
   6. api_key
   7. base_url
   8. model
2. 返回值
   1. type: 消息类型：user，ai，system，tool
   2. content: 通常是字符串，也有可能是字典列表，字典列表用于多模态输出
   3. name: 用来区分消息类型，不是所有模型都支持
   4. response_metadata：大模型响应附加的元数据，不同token有不同值，可能会包含本次token使用量等
   5. tools_calls：AI消息包含此属性，可通过.tool_calls返回toolCall列表，每个ToolCall是一个字典，包含如下字段：
      1. name：工具名称
      2. args： 工具参数
      3. id： 工具调用的唯一标识ID

```python
import os
from dotenv import load_dotenv
from langchain.chat_models import init_chat_model

load_dotenv(encoding='utf-8')
model2 = init_chat_model(
    model="deepseek-v3",
    model_provider="openai",
    api_key=os.getenv("QWEN_API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

# 3.调用模型
print(model2.invoke("你是谁").content)
```

## 流式输出

1. `model.invoke`，用来直接输出
2. `model.stream`，用来流式输出

## 设置日志取代print

```python
# 配置日志（可选，便于调试）
import logging

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)
```

## 带错误判断完整代码

1. 里面的raise，相当于through，用于抛出错误
2. 流式输出，要用for trunk in Content，用来遍历输出

```python
# 导入必要的库
from langchain_openai import ChatOpenAI
import os
from dotenv import load_dotenv
from langchain_core.exceptions import LangChainException

# 加载.env文件中的环境变量（指定编码，避免中文乱码）
load_dotenv(encoding='utf-8')

# 配置日志（可选，便于调试）
import logging

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)


def init_llm_client() -> ChatOpenAI:
    """
    初始化LLM客户端（封装成函数，提高复用性）

    Returns:
        ChatOpenAI: 初始化后的LLM客户端实例
    """
    # 1. 读取环境变量并做非空校验
    api_key = os.getenv("QWEN_API_KEY")
    if not api_key:
        raise ValueError("环境变量 QWEN_API_KEY 未配置，请检查.env文件")

    # 2. 初始化LLM客户端（参数命名规范，添加注释）
    llm = ChatOpenAI(
        model="deepseek-v3.2",  # 模型名称
        api_key=api_key,  # 通义千问API密钥
        base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",  # 阿里云兼容接口地址
        temperature=0.7,  # 可选：添加温度参数，控制输出随机性
        max_tokens=2048  # 可选：限制输出长度，避免超限
    )
    return llm


def main():
    """主函数：封装核心逻辑，符合Python工程化规范"""
    try:
        # 初始化客户端
        llm = init_llm_client()
        logger.info("LLM客户端初始化成功")

        # 调用模型（问题用变量存储，提高可读性）
        question = "你是谁"
        response = llm.invoke(question)

        # 格式化输出结果（而非直接打印原始对象）
        logger.info(f"问题：{question}")
        logger.info(f"回答：{response.content}")

        print("====================以下是流式输出,另一种调用方式")
        print("*" * 50)
        responseStream = llm.stream("介绍下langchain，300字以内")
        for chunk in responseStream:
            print(chunk.content,end="")
    # 捕获具体异常（而非宽泛的Exception）
    except ValueError as e:
        logger.error(f"配置错误：{str(e)}")
    except LangChainException as e:
        logger.error(f"模型调用失败：{str(e)}")
    except Exception as e:
        logger.error(f"未知错误：{str(e)}")


# 脚本入口（符合Python规范，避免导入时执行代码）
if __name__ == "__main__":
    main()
```







