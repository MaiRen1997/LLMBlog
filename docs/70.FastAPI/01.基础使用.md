## 启动命令

### 启动服务

1. main 是主入口的文件名
2. app是使用fastAPI实例化对象的名称
3. --reload 表示在代码修改时进行更新重启

```bash
uvicorn main:app --reload
```

### 查看接口文档

启动后，服务默认运行在localhost:8000上

如果需要查看文档，在后面加上/docs即可，即：localhost:8000/docs